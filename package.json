{
  "name": "debezium-diff",
  "version": "1.0.0",
  "description": "",
  "main": "out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "debezium-json-diff-viewer.viewBeforeAfterDiff",
        "title": "View Debezium Before and After"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./"
  },
  "engines": {
    "vscode": "^1.1.37"
  },
  "activationEvents": [
    "onLanguage:json",
    "onCommand:debezium-json-diff-viewer.viewBeforeAfterDiff"
  ],
  "keywords": [],
  "author": "swpark6",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/vscode": "^1.91.0",
    "typescript": "^5.5.3",
    "vscode": "^1.1.37"
  }
}